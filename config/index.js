// @flow

import { type Config } from '../types/config';

const config: Config = {
  siteName: 'Cactus Blog',
  slogan: '',
  layout: 'centered',
  theme: {
    colors: {
      main: '#24292E',
      dark: '#3F4448',
      light: '#FAFBFC',
      accent: '#5aac71',
    },
  },
};

export default config;
